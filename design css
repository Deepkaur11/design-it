* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: Arial, sans-serif;
  background-color: #f4f4f4;
}

header {
  background-color: #4a90e2;
  color: white;
  padding: 15px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.back-button {
  background: black;
  color: white;
  border: none;
  padding: 8px 12px;
  font-weight: bold;
  cursor: pointer;
}

.header-title {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 1.2em;
}

.icon {
  height: 30px;
}

main {
  padding: 20px;
  text-align: center;
}

h1 {
  margin-bottom: 20px;
}

.grid-container {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 15px;
  margin-bottom: 20px;
}

.card {
  background-color: #333;
  color: white;
  padding: 10px;
  border-radius: 5px;
}

.card img {
  width: 100%;
  height: 100px;
  object-fit: cover;
  border-radius: 3px;
}

.card p {
  margin-top: 8px;
  font-weight: bold;
}

.back-footer {
  background: black;
  color: white;
  padding: 10px 15px;
  border: none;
  font-weight: bold;
  margin-top: 10px;
}

footer {
  background-color: #4a90e2;
  color: white;
  padding: 10px;
  font-size: 0.9em;
}
@media (max-width: 600px) {
  .grid-container {
    grid-template-columns: 1fr;
  }
}
.card:hover {
  background-color: #555;
  cursor: pointer;
  transform: scale(1.02);
  transition: 0.3s ease;
}
main {
  padding: 20px;
  text-align: center;
  padding-bottom: 60px; /* to avoid sticking to footer */
}
